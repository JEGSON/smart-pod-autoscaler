apiVersion: 1

providers:
  - name: "SmartScaler"
    orgId: 1
    folder: "SmartScaler"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
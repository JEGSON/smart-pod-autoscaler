apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smart-pod-autoscaler.fullname" . }}-test-connection"
  labels:
    {{- include "smart-pod-autoscaler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smart-pod-autoscaler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

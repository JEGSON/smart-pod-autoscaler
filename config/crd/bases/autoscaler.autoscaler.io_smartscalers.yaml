---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: smartscalers.autoscaler.autoscaler.io
spec:
  group: autoscaler.autoscaler.io
  names:
    kind: SmartScaler
    listKind: SmartScalerList
    plural: smartscalers
    singular: smartscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetDeployment
      name: Target
      type: string
    - jsonPath: .spec.minReplicas
      name: Min
      type: integer
    - jsonPath: .spec.maxReplicas
      name: Max
      type: integer
    - jsonPath: .status.currentReplicas
      name: Replicas
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SmartScalerSpec defines the desired state of SmartScaler
            properties:
              maxReplicas:
                description: Maximum number of replicas
                format: int32
                type: integer
              metric:
                description: Metric source configuration
                properties:
                  endpoint:
                    description: The metric endpoint or connection URL
                    type: string
                  query:
                    description: Metric query (e.g Prometheus PromQL query or Kafka
                      topic name)
                    type: string
                  source:
                    description: 'Type of metric source: "prometheus", "kafka", "rabbitmq"'
                    type: string
                required:
                - endpoint
                - query
                - source
                type: object
              minReplicas:
                description: Minimum number of replicas
                format: int32
                type: integer
              scalingPolicy:
                description: Scaling thresholds and cooldown
                properties:
                  cooldownSeconds:
                    description: Seconds to wait before scaling again (prevents flapping)
                    format: int64
                    type: integer
                  scaleDownThreshold:
                    description: Scale DOWN when metric value per replica drops below
                      this
                    format: int64
                    type: integer
                  scaleUpThreshold:
                    description: Scale UP when metric value per replica exceeds this
                    format: int64
                    type: integer
                required:
                - cooldownSeconds
                - scaleDownThreshold
                - scaleUpThreshold
                type: object
              targetDeployment:
                description: The deployment this scaler will manage
                type: string
            required:
            - maxReplicas
            - metric
            - minReplicas
            - scalingPolicy
            - targetDeployment
            type: object
          status:
            description: SmartScalerStatus defines the observed state of SmartScaler
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentMetricValue:
                description: Current metric value being tracked
                format: int64
                type: integer
              currentReplicas:
                description: Current number of replicas
                format: int32
                type: integer
              lastScaleTime:
                description: Last time a scaling action was taken
                format: date-time
                type: string
              message:
                description: Human readable status message
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
